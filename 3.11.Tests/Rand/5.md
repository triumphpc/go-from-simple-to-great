### How do I get the random number generator to output a random number on every run?

1. rand.Seed(rand.Random)
2. rand.Seed([...])
3. rand.Seed(time.Now())
4. rand.Seed(time.Now().UnixNano())

>Answer: 4

